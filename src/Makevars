PKG_CFLAGS = $(LTO_OPT)
PKG_FFLAGS = $(LTO_OPT)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(LTO_OPT)

F_OBJS = Cheb.o
C_OBJS = Chebyshev.o compHorner.o init.o

all:
	@$(MAKE) $(SHLIB)
	@rm -f *.mod *.o

$(SHLIB): $(F_OBJS) $(C_OBJS)

init.o: Cheb.o

clean:
	@rm -f *.mod *.o
